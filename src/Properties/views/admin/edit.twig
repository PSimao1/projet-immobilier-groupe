{%  extends '@admin/layout.twig'%}

{% block body %}

    <form action="{{ path('properties.admin.edit', {id:item.id}) }}" method="POST">
        {% include '@properties/admin/form.twig' %}
        <div class="d-flex justify-content-center mt-4">
            <button class="btn btn-primary px-4 py-2" type="submit"> Modifier</button>
        </div>
    </form>

{% endblock %}