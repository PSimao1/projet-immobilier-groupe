{% extends "@admin/layout.twig" %}

{% block body %}

    <p class="text-right">
        <a href="{{ path('properties.admin.create') }}" class = "btn btn-primary">
            Ajouter un article
        </a>
    </p>

    <table class="table table-stripped">
        <thead>
        <tr>
            <td>Titre</td>
            <td>Ville</td>
            <td>Description</td>
        </tr>
        </thead>

        <tbody>
        {% for item in items %}
            <tr>
                <td>{{ item.title }}</td>
                <td>{{ item.city }}</td>
                <td>{{ item.description }}</td>
                <td>
                    <a href="{{ path('properties.admin.edit', {id:item.id}) }}" class="btn btn-primary">Editer</a>
                    <form style="display: inline" action="{{ path('properties.admin.delete', {id:item.id}) }}" method="POST" id="tata">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger">Supprimer</button>
                    </form>
                </td>
            </tr>
        {% endfor %}
        </tbody>

    </table>

    {{ paginate(items, 'properties.admin.index') }}

{% endblock %}